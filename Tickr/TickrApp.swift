import SwiftUI

@main
struct TickrApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Tickr — scaffold")
                .frame(minWidth: 480, minHeight: 320)
                .padding()
        }
    }
}
